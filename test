"\w.z ((\x.x) y) w -> \w.z y w" by E-AppFull {
    (w)[
        "z ((\x.x) w) w -> z w w" by E-App1 {
            "z ((\x.x) w) -> z w" by E-App2 {
                "(\x.x) w -> w" by E-AppAbs {}
            }
        }
    ]
}
